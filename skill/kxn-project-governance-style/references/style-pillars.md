# Style Pillars

## 1. Issue As Contract

Promotion note:

- promoted because multiple high-signal issues are written with explicit background, goals, scope, non-goals, and completion criteria

Matrix-backed anchors:

- `#40`
- `#70`
- `#140`
- `#160`
- `#180`
- `#200`
- `#220`
- `#227`

## 2. Shared Model First

Promotion note:

- promoted because the repository repeatedly extracts substrate or ownership boundaries before widening visible features

Matrix-backed anchors:

- `#70`
- `#160`
- `#180`

## 3. Staged Delivery

Promotion note:

- promoted because important work lands in waves: baseline, primary path, then hardening or alignment

Matrix-backed anchors:

- `#70`
- `#200`
- `#220`

## 4. Maintainability As Active Workstream

Promotion note:

- promoted because refactor and boundary cleanup are explicitly budgeted and phased, not hidden as opportunistic cleanup

Matrix-backed anchors:

- `#160`
- early maintainability stream `#2` through `#10`

## 5. Subtraction And Ownership Cleanup

Promotion note:

- promoted because new capabilities are commonly paired with route cleanup, bridge cleanup, or removal of old semantic paths

Matrix-backed anchors:

- `#70`
- `#160`

## 6. Risk And Failure Semantics In Mainline

Promotion note:

- promoted because fallback preview, deadlock avoidance, markdown rendering stability, and expiry handling are all treated as mainline product work

Matrix-backed anchors:

- `#220`
- `#227`

## 7. Delivery Governance

Promotion note:

- promoted because install, upgrade, admin route placement, and release-adjacent behavior are consistently treated as product infrastructure

Supporting anchors:

- route cleanup around `/admin`
- installer and upgrade work across the commit stream

## 8. Docs And Tests As Boundary Locks

Promotion note:

- promoted because docs and tests are repeatedly updated alongside state-machine or behavior shifts, not after the fact

Matrix-backed anchors:

- `#40`
- `#140`
- `#160`
- `#180`
- `#200`
- `#220`
- `#227`

## 9. Product Language And Information Architecture Governance

Promotion note:

- promoted because setup/onboarding flow, route naming, follow-up links, and interaction semantics are actively redesigned to match the intended user model

Matrix-backed anchors:

- `#140`
- `#200`

Supporting anchors:

- wording and route harmonization commits around setup/admin
