# Evidence Index

## Matrix-Backed Anchors

These items are backed by explicit issue -> commit -> code/doc/test chains in `evidence-matrix.md`.

- `#40` issue/doc sync workflow
- `#70` workspace-first thread ownership
- `#140` detection-driven onboarding and setup information architecture
- `#160` Feishu UI query / policy / DTO boundary extraction
- `#180` shared token usage substrate before footer rendering
- `#200` staged interaction alignment for `request_user_input`
- `#220` unified preview fallback with lifecycle and concurrency hardening
- `#227` markdown link stabilization without rollback to the legacy path

## Supporting Anchors

These support the same style conclusions but are not expanded into full matrix rows here.

- early maintainability stream `#2` through `#10`
- route and wording cleanup around `/admin`
- release and installer governance through the April 2026 commit stream
- state-machine docs and workflow guardrails across `.codex/` and `docs/general/`

## Coverage Note

The matrix is intentionally selective. It shows strong exemplars across contract writing, staged delivery, maintainability, risk handling, docs/tests, and information architecture governance. It is not a claim that every high-level style statement is backed by a single exhaustive chain.
